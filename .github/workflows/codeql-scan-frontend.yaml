name: CodeQL Scan (frontend)

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

env:
  TZ: Europe/Oslo

jobs:
  codeql-scan:
    permissions:
      contents: read
      security-events: write
    name: CodeQL Scan
    uses: navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@6944959544fb135a3b101ba0078a8fef8870568e # ratchet:navikt/mob-gha-workflows/.github/workflows/codeql-scan.yaml@v1.1.2
    secrets: inherit
    with:
      language: 'javascript'
