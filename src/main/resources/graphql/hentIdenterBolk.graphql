query($identer: [ID!]!) {
    hentIdenterBolk(identer: $identer, historikk: true, grupper: [FOLKEREGISTERIDENT]) {
        ident,
        identer {
            ident
            historisk
            gruppe
        }
    }
}
